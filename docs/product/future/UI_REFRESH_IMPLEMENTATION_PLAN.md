# План реализации: визуальный рефреш «Оргструктура» и «Чек-лист-сессии»

**Основание:** прототипы `docs/lms-ui-prototypes-complete/admin/lms-admin-org-structure-refresh.html`
(оргструктура и shell приложения) и `docs/lms-ui-prototypes-complete/admin/lms-checklist-sessions-refresh.html`
(чек-лист-сессии). Они зарегистрированы в `docs/lms-ui-prototypes-complete/manifest.json`: записи
`admin-org-*` и `*-checklist-*` со статусом `parityStatus: diverged` и известными отличиями. Прежний
прототип `/admin/organization` архивирован в `docs/archive/old-trackers/`. Правила работы с прототипами —
`docs/lms-ui-prototypes-complete/README.md`.

**Статус:** реализация начата. PR 309 (API счётчиков разделов оргструктуры) выполнен: `GET /api/v1/org-structure/counts`, клиент `getOrgStructureCounts()`. PR 310 (shell приложения) в работе. Остальные PR не начаты.

**Решения владельца продукта (2026-09-26):**
- shell (сайдбар и топбар) приводится к прототипу во всём приложении, а не только в оргструктуре;
- мобильные таблицы чинятся на всех экранах, а не только в оргструктуре;
- счётчики на вкладках оргструктуры отдаёт один новый лёгкий серверный запрос.

## 0. Границы — обязательно к соблюдению

- Рефреш только визуальный. Дерево подразделений, должности, требования, группы, CSV-импорт с историей и
  цикл чек-лист-сессий уже реализованы и не переписываются. Весь существующий функционал (поиск, фильтры,
  действия, карточка подразделения, архивирование) сохраняется, даже если в прототипе его нет.
- Палитра не меняется. Основной цвет и фон — из org-theme (`apps/web/src/shared/theme.ts`,
  `defaultThemeSettings`), статусные цвета — из `apps/web/src/styles/tokens.css`, шрифт — Manrope
  (`apps/web/src/styles/global.css`). Ни один PR не меняет `tokens.css` и `defaultThemeSettings`.
- Тёмная тема на HTML-страницах прототипов — только оформление просмотрщика. В приложении тёмной темы нет,
  этот план её не вводит.
- Новый бэкенд — эндпоинт счётчиков (PR 309) и хранение значений `contextFields` (PR 323). Остальные
  PR — только `apps/web`.
- Каждый PR меняет shared UI или layout, поэтому по `AGENTS.md` обязательны `pnpm test:visual` и
  `pnpm test:a11y`. Baselines обновляются только через `.github/workflows/update-visual-baselines.yml` из
  ветки с актуальным `main`.
- Проверку изменённых эталонных PNG делает агент, который ведёт PR, а не владелец продукта. После
  обновления baselines агент подтягивает коммит workflow, открывает каждый изменённый PNG и сравнивает его
  с прежней версией из `git show HEAD~1:<путь>`. В описании PR он перечисляет, какие экраны изменились
  и почему. Если изменился экран, который PR не трогал, это регрессия: агент исправляет её до merge, а не
  принимает новый baseline.
- После реализации агент сравнивает свои экраны с прототипом и обновляет их записи в
  `docs/lms-ui-prototypes-complete/manifest.json`: `parityStatus: aligned`, `lastComparedAt`,
  `lastComparedSha`, очищенные `knownDifferences`. Если отличие осталось сознательно, оно остаётся в
  `knownDifferences` с причиной.

## 0.1. Функциональный пробел, который закрывает этот план

`ChecklistBuilder` сохраняет определения `contextFields` листа («Общая информация»: текст, многострочный
текст, дата; обязательное или нет), но `ChecklistSessionConduct.tsx` их не рендерит, а значения негде
хранить. `CHECKLIST_WORKPLACE_TRAINING_IMPLEMENTATION_PLAN.md` (PR 297) оставил это как gap «для
будущего PR». Этот план закрывает его в PR 323.

## 0.2. Текущее состояние оргструктуры (проверено 2026-09-26 на `main` `654908d`)

Все 5 вкладок отрендерены локально под администратором и сравнены с прототипом.

- Вкладки — `OrgStructureTabs` в `apps/web/src/shared/adminPage.tsx`, обычные `<a href>` на 5 роутов.
  Каждый клик перезагружает страницу. Визуально это совпадает с прототипом, переделка на client-side
  панели не требуется.
- У каждой вкладки свой заголовок и разный eyebrow («Подразделения», «Оргструктура», «Группы»,
  «Organization structure»). В прототипе один общий заголовок раздела и счётчики на вкладках.
- «Импорт и история» (`AdminOrgStructureToolsPage.tsx`) полностью на английском при RU-локали: строки
  захардкожены, `brandLabel="LearnSpace"` и свои `navItems` вместо `t(...)`. В колонке автора — полный UUID.
- Mobile 390: таблицы `DataTable` без `data-priority` на колонках (должности, требования, группы, курсы)
  сжимают колонки до ~35px, текст переносится по одной букве. На `/admin/users` приоритеты заданы, там
  таблица читается.
- Карточка выбранного подразделения на 1440px: ряд действий выходит за край карточки на 52px. На mobile
  кнопка «+ Добавить корневое подразделение» обрезана краем экрана.

---

## PR 309 — API: счётчики разделов оргструктуры ✅

**Цель:** один лёгкий запрос отдаёт числа для всех пяти вкладок, без загрузки чужих списков.

**Зависимости:** нет.

**Что необходимо сделать:**
- `GET /api/v1/org-structure/counts` в существующем модуле `org-structure-admin`
  (`org-structure-admin.controller.ts` / `.service.ts`), без нового модуля.
- Ответ — пять чисел: `departments`, `positions`, `positionCourses`, `groups`, `historyEvents`.
  Считаются только активные записи организации текущего пользователя (без архивных и удалённых), история —
  все записи `OrgStructureEvent` организации.
- Только `count()`-запросы Prisma, без выборки строк.
- Доступ — `rolePolicies.departmentsRead`, как у `GET /org-structure/history`.
- Клиент: `getOrgStructureCounts()` в `apps/web/src/shared/api/org-structure-admin.ts`.
- Перегенерировать `docs/generated` через `pnpm docs:generate`, проверить `docs/contracts/API_CONTRACTS.md`
  и `docs/contracts/API_RBAC_MATRIX.md`.

**Критерии готовности:**
- [x] unit-тест сервиса: архивные записи не учитываются, чужая организация не учитывается — `org-structure-admin.service.spec.ts` (фильтры запросов) и `org-structure-admin.database.spec.ts` (реальная БД: архивные, удалённые группы и чужая организация не попадают в счёт);
- [x] тест контроллера: роль без `departmentsRead` получает 403 — `org-structure-admin.controller.spec.ts` через `RolesGuard`, плюс общий `api-policy.audit.spec.ts`;
- [x] `pnpm docs:generate:check` и `pnpm docs:consistency:test` зелёные.

## PR 310 — Shell приложения: сайдбар и топбар как в прототипе

**Цель:** все рабочие пространства получают навигацию из прототипа.

**Зависимости:** нет.

**Что необходимо сделать:**
- Два shell-компонента: `AdminPageLayout` (`shared/adminPage.tsx`, 21 admin-страница) и `WorkspaceLayout`
  (`shared/workspaceLayout.tsx`, общий для `learnerLayout`, `instructorLayout`, `managerLayout`,
  `mentorLayout`).
- Иконка у каждого пункта меню. Inline SVG в одном месте (новый `shared/navIcons.tsx`), без новой
  зависимости.
- Активный пункт залит основным цветом org-theme, текст белый (как `.nav-item.is-active` в прототипе).
- Подписи групп меню («Управление», «Настройки») — мелкий uppercase с разрядкой.
- Внизу сайдбара — аватар с инициалами, имя и email текущего пользователя.
- Топбар — хлебные крошки (раздел / подраздел / текущая страница) вместо одного заголовка. Кнопки
  уведомлений, смены аккаунта, языка и выхода остаются.
- Mobile: hamburger и выезжающее меню сохраняются, в выезжающем меню — те же иконки и активный стиль.
- Instructor workspace сейчас с верхней навигацией вместо сайдбара — перевести на тот же сайдбар, что у
  остальных ролей.
- Единый бренд в шапке сайдбара. Сейчас он разный: «LearnSpace / Админ», «LMS / Учебная платформа»
  (ученик), «LMS / Менеджер».
- Активный пункт меню подсвечивается и на вложенных роутах: сейчас на `/admin/checklists/sessions` и
  `/admin/checklists/sessions/:id` пункт «Чек-листы» не активен.
- Хлебные крошки берутся из маршрута, а не из `navItems` страницы. Сейчас у билдера чек-листа крошки
  «Курсы / Конструктор тестов / Чек-листы».
- Mobile 390: верхняя панель не выходит за экран. Сейчас у инструктора кнопка «Выйти» и аватар уходят
  за край (правая граница 462px).

**Критерии готовности:**
- [x] все admin-страницы и все четыре роли-workspace получают новый shell — `AdminPageLayout` и `WorkspaceLayout` (instructor и mentor переведены с верхнего меню на сайдбар), общие `shared/navIcons.tsx` и `shared/navigationDrawer.ts`;
- [x] на 390px ни у одной роли элементы шапки не выходят за экран — замер в браузере для admin, learner, instructor, manager, mentor; у learner заодно исправлена колонка `.learner-app` (`minmax(0, 1fr)`), которая на `main` растягивалась до 430px;
- [x] у каждой страницы корректные хлебные крошки на всех 4 локалях (`ru`, `en`, `kk`, `zh`) — admin: раздел / пункт меню / вложенная страница из маршрута (`buildAdminBreadcrumbs`), workspace: роль / пункт меню; все подписи из i18n, добавлен ключ `a11y.mainNavigation`;
- [x] фокус с клавиатуры виден на пунктах меню, `pnpm test:a11y` зелёный для shell — проверки навигации и фокуса проходят; 4 проверки страниц с бейджами статусов падают на локальных данных с сессиями одинаково и на `main` (контраст `.ds-badge--info`/`--success` и `.ui-status-badge` из `tokens.css`), в CI без таких данных не воспроизводится — это отдельный дефект палитры вне этого плана;
- [x] `adminPage.spec.tsx` и `workspaceLayout.spec.tsx` обновлены;
- [x] visual baselines обновлены через workflow и просмотрены — 25 эталонов instructor/learner/manager пересняты `update-visual-baselines.yml` и просмотрены попарно: меняется только shell; на 320–375 px остаётся давнее переполнение контента страниц (ряды фильтров instructor/learner, карточки manager), это PR 311/319.

## PR 311 — Мобильные таблицы на всех экранах

**Статус:** реализация завершена; visual baselines должны быть обновлены и просмотрены только через
`.github/workflows/update-visual-baselines.yml` после push ветки PR.

**Цель:** на ширине телефона таблицы читаемы на всех страницах.

**Зависимости:** нет.

**Что необходимо сделать:**
- Найти все `DataTable` без `data-priority` на колонках (минимум: должности, требования по должностям,
  группы, курсы) и разметить колонки как основные и второстепенные по образцу `/admin/users`.
  Второстепенные на ≤860px уходят в раскрытие строки.
- Колонку действий не давать сжимать остальные: на mobile действия уходят в раскрытие строки или в одну
  кнопку-меню.
- Общее правило `.ds-data-table .admin-table-wrap table { min-width: 0 }` в `styles/ui.css` менять только
  если разметка приоритетов не решает задачу на каком-то экране.

**Критерии готовности:**
- [x] на 390px ни на одной странице текст в ячейке не переносится по буквам;
- [x] на страницах нет горизонтального скролла body;
- [ ] visual regression mobile-матрицы (`responsive-matrix.spec.ts`) обновлена и просмотрена.

## PR 312 — Оргструктура: общая шапка раздела и вкладки со счётчиками

**Цель:** 5 вкладок выглядят как одна страница «Организационная структура».

**Зависимости:** PR 309, PR 310.

**Что необходимо сделать:**
- Новый компонент шапки раздела в `shared/adminPage.tsx` рядом с `OrgStructureTabs`: eyebrow
  «Настройки», h1 «Организационная структура», описание раздела. Используется всеми 5 страницами вместо их
  собственных `AdminPageHeader`.
- Вкладки под шапкой, у каждой счётчик-пилюля из `GET /org-structure/counts`. Пока счётчики грузятся,
  пилюля не показывается, ошибка запроса не ломает вкладки.
- Кнопки действий страниц («+ Добавить должность» и т.п.) переезжают в шапки карточек (PR 313–316).
- Mobile: вкладки в одну строку с горизонтальным скроллом (`overflow-x: auto`, `white-space: nowrap`), а
  не перенос в 3 строки.

**Критерии готовности:**
- [ ] на всех 5 вкладках одинаковая шапка и eyebrow;
- [ ] счётчики совпадают с данными вкладок;
- [ ] на 390px вкладки в одну строку, body не скроллится по горизонтали;
- [ ] активная вкладка сохраняет `aria-current="page"`.

## PR 313 — Оргструктура: должности, требования, группы

**Цель:** три табличные вкладки в стиле карточек прототипа.

**Зависимости:** PR 311, PR 312.

**Что необходимо сделать:**
- `AdminPositionsPage.tsx`: карточка «Справочник должностей» с подзаголовком и кнопкой «+ Должность» в
  шапке. Колонки: должность (жирным), код (моноширинным, приглушённым), статус (бейдж).
- `AdminPositionCoursesPage.tsx`: карточка «Требования к обучению по должностям». Требование — бейдж
  («Обязательный» акцентным, «Опционально» приглушённым), срок в днях выровнен вправо, `—` без срока.
- `AdminGroupsPage.tsx`: информационная плашка «Группа — учебная команда или когорта, а не штатное
  подразделение…», карточка «Учебные группы» с подзаголовком «N групп». Колонка участников выровнена
  вправо, руководитель — аватар с инициалами.
- Поиск, фильтр статуса, stat-карточки групп и кнопки действий сохраняются в карточках.

**Критерии готовности:**
- [ ] три вкладки визуально соответствуют прототипу на 1440px;
- [ ] все существующие действия работают, e2e оргструктуры зелёные;
- [ ] visual baselines обновлены и просмотрены.

## PR 314 — Оргструктура: дерево подразделений

**Цель:** дерево в стиле прототипа при сохранении карточки выбранного подразделения.

**Зависимости:** PR 312.

**Что необходимо сделать:**
- `AdminDepartmentsPage.tsx` и `admin-departments/DepartmentTree.tsx`: дерево в карточке «Дерево
  подразделений» с подзаголовком «N подразделений», поиск и кнопка «Развернуть всё» в одной строке.
- Строка дерева: стрелка-иконка вместо `▸/▾`, название жирным, код моноширинным, справа аватары
  руководителей с инициалами и численность «N сотр.».
- Численность в строке берётся только из данных, которые дерево уже получает. Если их нет — показывать её
  только в карточке подразделения, без новых запросов на каждый узел.
- Карточка выбранного подразделения остаётся справа. Ряд действий переносится внутри карточки, не выходит
  за край.
- Mobile: кнопки шапки («Типы подразделений», «+ Подразделение») переносятся и не обрезаются.

**Критерии готовности:**
- [ ] «Развернуть всё» раскрывает все уровни, клавиатурная навигация по дереву не ломается;
- [ ] на 1440px ни одна кнопка не выходит за карточку;
- [ ] на 390px кнопки шапки видны полностью;
- [ ] `admin-departments.spec.ts` и `admin-department-managers.spec.ts` зелёные.

## PR 315 — Оргструктура: «Импорт и история»

**Цель:** экран локализован и собран в одну карточку, как в прототипе.

**Зависимости:** PR 312.

**Что необходимо сделать:**
- `AdminOrgStructureToolsPage.tsx`: все строки через `t(...)` с ключами в `ru`, `en`, `kk`, `zh`.
  `brandLabel`, `sidebarLabel` и навигация — как на остальных вкладках.
- Одна карточка «Импорт оргструктуры»: выбор типа и режима, зона загрузки CSV, предпросмотр с ошибками по
  строкам, кнопка применения.
- Ниже в той же карточке — «История изменений»: дата в локали пользователя, изменение, сущность, автор.
  Автор — имя пользователя (или «Система»), а не UUID.
- Пагинация истории локализована.

**Критерии готовности:**
- [ ] в `ru-RU` на экране нет английских строк;
- [ ] импорт превью → commit работает как раньше;
- [ ] автор в истории показан именем.

---

## 0.3. Текущее состояние чек-лист-сессий (проверено 2026-09-26 на `main` `654908d`)

Все 6 экранов прототипа `lms-checklist-sessions-refresh.html` отрендерены локально под
admin, instructor, learner и manager на тестовом чек-листе с группами критериев и сессиями в статусах
«запланирована», «в процессе» и «завершена». Визард создания сравнивался как диалог экрана сессий.

- **Билдер** (`features/admin-checklists/ChecklistBuilder.tsx`): поле текста критерия имеет ширину 0px
  (замер на 1440px), текст критериев не виден и не редактируется. Строка критерия — набор инпутов
  (баллы, вес, шкала обрезана до «Без ш», чекбоксы), а не таблица «Критерий / Шкала / Обязательный /
  Фото» с группами-карточками, как в прототипе.
- **Список сессий** (`AdminChecklistSessionsPage.tsx`): статус — select вместо вкладок «Все /
  Запланированные / В процессе / Завершённые / Отменённые». Нет фильтров «Период» и «Наблюдатель», хотя
  `GET /checklist-sessions` уже принимает `scheduledFrom`, `scheduledTo` и `observerId`. Дата в формате
  «26 сент. 2026 г., 08:20» вместо «26.09.2026 08:20». Кнопки действий переносятся в две строки.
  Заголовок «Контроль знаний / Сессии» вместо «Обучение на рабочем месте / Обучающие сессии».
- **Визард**: в строках выбора наблюдателя и сотрудников радио/чекбокс стоит по центру, а имя прижато
  вправо и переносится («Iris / Instructor»).
- **Проведение** (`ChecklistSessionConduct.tsx`): открывается не отдельным экраном, а внутри страницы
  «Проверка чек-листов» под пятью KPI-карточками проверки. Форма обратной связи видна всё время под
  критерием. Кнопки «Пауза / Завершить» стоят над критерием, «Назад / Далее» — под ним; в прототипе вся
  навигация внизу. Список сессий инструктора — вкладка «Проведение» среди фильтров очереди проверки.
- **«Мои сессии»** (`LearnerChecklistSessions.tsx`): карточки и pill-фильтры вместо таблицы «Сессия /
  Наблюдатель / Дата / Результат» с вкладками. Обратная связь наблюдателя видна только внутри сессии,
  блока «Предыдущая обратная связь» на экране нет.
- **Дашборд руководителя** (`ManagerChecklistsPage.tsx`): структура совпадает с прототипом (KPI, donut,
  динамика, таблица сотрудников). Шестая KPI-карточка стоит одна во втором ряду.
- **Отчёт** (`AdminChecklistSessionReportPage.tsx`): вкладки — залитые кнопки, активная почти не
  отличается. «Сводка» — список «метка: значение», а не три KPI-карточки (результат, сильные стороны,
  зоны развития). «Пересчитать» спрятано в форме на вкладке «История», а не в шапке. Во вкладке
  «Критерии» нет результата по критерию (выполнен / пропущен / баллы). Геолокация выводится сырым
  значением `off`.
- **Нелокализованные строки в RU**: «Reassign observer», «Required item», «Required», «Due date»,
  «Optional. Enter local time; it is stored as UTC.», «Can't conduct this», заголовок колонки «DETAILS».
  Ключей `admin.checklists.field.isRequired`, `admin.checklists.sessions.reassignObserver`,
  `admin.checklists.field.dueAt`, `admin.checklists.dueAtHint` нет в `i18n/locales/ru/common.json`.

Shell (PR 310) и мобильные таблицы (PR 311) покрывают эти экраны без отдельных PR.

## PR 316 — Чек-листы: локализация модуля

**Цель:** в RU на экранах чек-листов и сессий нет английских строк.

**Зависимости:** нет.

**Что необходимо сделать:**
- Добавить недостающие ключи из раздела 0.3 во все локали (`ru`, `en`, `kk`, `zh`).
- Геолокацию в отчёте выводить через перевод значения (`off` → «Выключено» и т.д.), а не сырым enum.
- Unit-тест: все ключи `admin.checklists.*` и `checklistSessions.*`, используемые в компонентах модуля,
  есть в `ru/common.json`.

**Критерии готовности:**
- [ ] на 6 экранах модуля в `ru-RU` нет английских строк;
- [ ] тест на наличие ключей зелёный.

## PR 317 — Билдер листа наблюдения

**Цель:** билдер работает и выглядит как в прототипе.

**Зависимости:** PR 316.

**Что необходимо сделать:**
- Исправить нулевую ширину поля текста критерия — это дефект, не стиль, исправить первым.
- Каждая группа — карточка: название, «N критериев», кнопки «+ Критерий», копирования и перемещения.
- Критерии группы — таблица: перетаскивание (или стрелки порядка), критерий, шкала, «Обязательный» и
  «Фото» переключателями. Баллы и вес — колонками, только когда их требует режим оценки. Удаление — в
  меню строки.
- Правая колонка — «Настройки листа»: система оценки, порог, подтверждение инструктором, геолокация,
  видимость до сессии. Блок «Назначение» остаётся под ней.
- Шапка: eyebrow «Лист наблюдения», название, бейдж статуса, «Шкалы оценки» и «Предпросмотр» справа.
- «Общая информация» (`contextFields`) остаётся как есть; функциональный пробел из раздела 0.1 не
  трогается.

**Критерии готовности:**
- [ ] на 1440px текст каждого критерия виден и редактируется;
- [ ] все существующие действия билдера работают, `AdminChecklistsPage.builder.spec.tsx` обновлён;
- [ ] visual baselines обновлены и просмотрены.

## PR 318 — Список сессий и визард создания

**Цель:** экран «Обучающие сессии» как в прототипе.

**Зависимости:** PR 311, PR 316.

**Что необходимо сделать:**
- Шапка: eyebrow «Обучение на рабочем месте», h1 «Обучающие сессии», подзаголовок «Планирование,
  проведение и контроль обучения», кнопки «Настройки модуля» и «+ Новая сессия».
- Вкладки статусов «Все / Запланированные / В процессе / Завершённые / Отменённые» вместо select.
- Карточка фильтров: «Период», «Наблюдатель», «Поиск» через уже существующие параметры
  `GET /checklist-sessions`.
- Дата — `ДД.ММ.ГГГГ ЧЧ:ММ` в часовом поясе сессии. Название чек-листа жирным, статус бейджем,
  результат процентом.
- Действия строки («Отчёт», «Повторить», «Отменить», «Сменить наблюдателя») — одна кнопка-меню, чтобы
  строка не переносилась.
- Визард: строка выбора — контрол слева, имя и email рядом, без центрирования и переноса имени.

**Критерии готовности:**
- [ ] фильтры по периоду и наблюдателю реально сужают список;
- [ ] действия строки в одну строку на 1440px;
- [ ] визард: имена участников в одну строку, выбор работает с клавиатуры.

## PR 319 — Проведение сессии (observer)

**Цель:** mobile-first экран проведения, как в прототипе.

**Зависимости:** PR 310, PR 316.

**Что необходимо сделать:**
- Отдельный роут проведения (например, `/instructor/checklists/sessions/:id`) вместо встраивания в
  страницу «Проверка чек-листов» под KPI проверки.
- Список сессий инструктора — отдельная секция «Сессии к проведению» со статусом, сотрудником и датой.
  Строка открывается кнопкой, а не только кликом по карточке.
- Экран проведения: название листа, сотрудник и таймер, прогресс, «Критерий N из M», уведомление о
  геопозиции.
- Карточка критерия: название, «Обязательный · шкала», кнопки шкалы 1–N (или «Выполнено» для режима
  баллов), комментарий, «Добавить фото» и «Пропустить» внутри карточки.
- Внизу одна панель: «Назад», «Пауза», «Далее». На последнем критерии «Далее» становится «Завершить».
- Структурированная обратная связь — шаг после последнего критерия, а не постоянный блок под карточкой.

**Критерии готовности:**
- [ ] на 390px весь экран без горизонтального скролла, кнопки не меньше 44px;
- [ ] пауза, пропуск, фото, геолокация и завершение работают как раньше;
- [ ] `checklist-session-lifecycle.spec.ts`, `ChecklistSessionConduct.spec.tsx` зелёные.

## PR 320 — «Мои сессии» сотрудника

**Цель:** экран сотрудника как в прототипе.

**Зависимости:** PR 316.

**Что необходимо сделать:**
- Вкладки «Запланированные / В процессе / Завершённые» вместо pill-фильтров.
- Таблица «Сессия / Наблюдатель / Дата / Результат»: результат — бейдж статуса или процент.
- Карточка «Предыдущая обратная связь» с сильными сторонами и зонами развития последней завершённой
  сессии, данные — из уже загруженного списка сессий.
- Правило видимости результата и обратной связи (`feedbackVisibility`) не меняется.

**Критерии готовности:**
- [ ] при `after_completion` обратная связь не показывается до завершения сессии;
- [ ] `LearnerChecklistSessions.spec.tsx` обновлён.

## PR 321 — Дашборд руководителя

**Цель:** выровнять сетку дашборда с прототипом.

**Зависимости:** PR 316.

**Что необходимо сделать:**
- KPI-карточки в одну ровную сетку без одиночной карточки во втором ряду.
- Подписи KPI как в прототипе: «Низкое выполнение», «Высокое выполнение», «Без сессий за период»,
  «Средний результат». Пояснение порога при незаданном `lowThreshold` — через иконку-подсказку.
- В таблице сотрудников колонка «Тренд» со стрелками ↑/↓.

**Критерии готовности:**
- [ ] сетка KPI ровная на 1440px и 390px;
- [ ] `ManagerChecklistsPage.spec.tsx` зелёный.

## PR 322 — Отчёт по сессии

**Цель:** отчёт как в прототипе.

**Зависимости:** PR 316.

**Что необходимо сделать:**
- Шапка: eyebrow «Отчёт по сессии», название листа, «дата · сотрудник», справа бейдж статуса, «Печать»
  и «Пересчитать». «Пересчитать» открывает текущую форму с причиной в диалоге.
- Вкладки «Сводка / Участники / Критерии / Файлы / История» — подчёркиванием, как во всём приложении.
- «Сводка»: три KPI-карточки — итоговый результат с бейджем, сильные стороны, зоны развития; ниже
  «История изменений результата» таймлайном.
- «Критерии»: у каждого критерия результат — выполнен, не выполнен, пропущен, баллы или уровень шкалы.

**Критерии готовности:**
- [ ] пересчёт с причиной работает из шапки, ревизии видны в истории;
- [ ] `AdminChecklistSessionReportPage.spec.tsx` обновлён.

## PR 323 — Контекстные поля сессии (`contextFields`)

**Цель:** поля «Общая информация» из билдера заполняются при проведении, сохраняются и видны в отчёте.

**Зависимости:** PR 319.

**Что необходимо сделать:**
- Prisma: хранение значений на сессии, например `ChecklistSession.contextValues Json?` — объект
  `{ [fieldId]: string }`. Миграция только добавляет nullable-колонку; существующие сессии не трогаются.
- Определения полей фиксируются на момент старта сессии. Если админ изменит поля листа после начала
  сессии, сохранённые значения не должны терять подписи. Способ (снэпшот определений в сессии или
  ссылка по `id` с сохранением подписи) описать в PR.
- API: `PATCH /checklist-sessions/:id/context` с `version` и тем же 409-контрактом, что у feedback.
  Разрешено в статусах `in_progress` и `paused`. Проверка: `id` поля существует в листе, значение
  подходит типу (`date` — ISO-дата), длина ограничена.
- Завершение сессии (`complete`) отклоняется, если не заполнено обязательное контекстное поле, — на
  сервере, а не только в UI.
- RBAC как у проведения (`checklistSessionsRun`). Значения видят те же роли, что видят сессию; для
  сотрудника — по тому же правилу `feedbackVisibility`.
- UI проведения: первый шаг «Общая информация» перед критериями, поля по типу, обязательные помечены,
  «Далее» блокируется, пока обязательные пустые.
- Отчёт по сессии: блок «Общая информация» во вкладке «Сводка».
- Обновить `docs/contracts/API_CONTRACTS.md`, `docs/contracts/API_RBAC_MATRIX.md`, перегенерировать
  `docs/generated` через `pnpm docs:generate`.

**Критерии готовности:**
- [ ] значения сохраняются и видны в отчёте после перезагрузки;
- [ ] без обязательного поля `complete` возвращает 400;
- [ ] stale `version` даёт 409, как у остальных мутаций сессии;
- [ ] database-тест миграции и сервиса, e2e сценарий проведения с контекстными полями;
- [ ] `pnpm docs:generate:check` и `pnpm docs:consistency:test` зелёные.

# Definition of Done

- [ ] PR 309–323 смёржены;
- [ ] ни один PR не менял `apps/web/src/styles/tokens.css` или `defaultThemeSettings`;
- [ ] обязательный CI зелёный на каждом PR, visual baselines обновлены через workflow и просмотрены агентом;
- [ ] у всех записей плана в manifest `parityStatus: aligned` или сознательные отличия в `knownDifferences`;
- [ ] `pnpm docs:consistency:test` зелёный.
