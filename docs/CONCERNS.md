# Concerns Register

Файл для фиксации всего что вызывает сомнение — архитектура, потенциальные проблемы, технический долг, наблюдения по PR.

**Severity:** 🔴 критично · 🟡 смущает · 🟢 мелочь  
**Status:** открыто / закрыто (дата)

---

## Открытые

### [2026-07-31] `assertResourceAccess` — двойной запрос в БД для вложенных ресурсов
**Файл:** `apps/api/src/modules/course-access/course-access.policy.ts`  
**Severity:** 🟡  
Для `attempt` и `question` метод делает 2 запроса: сначала резолвит `courseId` через промежуточную таблицу, потом проверяет доступ к курсу через `assertCourseAccess`. При высокой нагрузке это заметно. Можно объединить в один Prisma-запрос с `include`/JOIN.

---

### [2026-07-31] Миграции с хардкоженным timestamp в имени
**Файлы:** `apps/api/prisma/migrations/20260731120000_*`, `20260731150000_*`  
**Severity:** 🟢  
Если разработчик запустит `prisma migrate dev` локально в тот же день — возможен конфликт порядка применения миграций. Мелочь, но стоит следить.

---

### [2026-07-31] RBAC-тесты есть только у 2 из 14 контроллеров
**Файлы:** `apps/api/src/modules/assignments/`, `apps/api/src/modules/progress/`  
**Severity:** 🟡  
Audit-тест (`api-policy.audit.spec.ts`) проверяет что у каждого endpoint есть `@Roles()` — но это не то же самое что проверить поведение guard. Dedicated `.rbac.spec.ts` файлы есть только у `assignments` и `progress`. Остальные 12 контроллеров без поведенческих RBAC-тестов.

---

### [2026-07-31] Redis rate limiting не реализован — счётчики in-memory
**Файл:** `apps/api/src/modules/auth/`  
**Severity:** 🟡  
PR 123 в плане. Текущий rate limit хранится в памяти процесса. При перезапуске NestJS на Railway — счётчики сбрасываются. При горизонтальном масштабировании (несколько инстансов) — счётчики не синхронизированы. На продакшне это дыра в защите от брутфорса.

---

### [2026-07-31] S3/R2 инфраструктура на Railway не настроена
**Файл:** `apps/api/src/modules/` (upload-модуль)  
**Severity:** 🟡  
PR 170 — код загрузки файлов написан, но бакет S3/R2 на Railway не создан и env-переменные не выставлены. Функциональность загрузки материалов сейчас нерабочая в проде.

---

### [2026-07-31] Покрытие функций на нижней границе
**Файл:** `apps/web/vitest.config.ts`  
**Severity:** 🟢  
Threshold `functions: 25%`. После фикса PR 172 — 25.6%, запас минимальный. Любое добавление нетестированных функций в `apps/web` уронит CI. Стоит поднять порог до 30% или добавить тесты заранее.

---

### [2026-07-31] Instructor видит все курсы организации до PR 176
**Файл:** `apps/web/src/app/InstructorDashboardPage.tsx`, `InstructorCourseStudentsPage.tsx`  
**Severity:** 🟡  
Фронтенд инструктора вызывает `listCourses({ pageSize: 200 })` без фильтрации по владельцу. API-сайд ownership (`CourseAccessPolicy`) добавлен в PR 176, но фронтенд ещё не обновлён — инструктор в UI видит все курсы организации.

---

### [2026-07-31] `sessionStore` @Optional — logout-all молча не работает без Redis
**Файл:** `apps/api/src/modules/auth/auth.controller.ts`  
**Severity:** 🔴  
`AuthSessionStore` помечен `@Optional()`. Если Redis не сконфигурирован — `logout-all` возвращает `{ accepted: true }` но сессии не отзывает. Пользователь считает что вышел со всех устройств — нет.

---

### [2026-07-31] `ALLOW_IN_MEMORY_RATE_LIMIT=true` — escape хatch без предупреждения
**Файл:** `apps/api/src/config/env.ts`  
**Severity:** 🟡  
Флаг позволяет запустить production без Redis. Нет Warning в логах при старте что включён небезопасный режим. Легко выставить на Railway и забыть.

---

### [2026-07-31] Fail-open при недоступном Redis — rate limit полностью отключается
**Файл:** `apps/api/src/common/middleware/api-hardening.ts`  
**Severity:** 🟡  
При ошибке Redis store middleware делает `catch {}` и пропускает запрос. Атакующий может положить Redis и брутфорсить `/auth/login` без ограничений.

---

### [2026-07-31] `createCourse` + `assignInstructor` без транзакции
**Файл:** `apps/api/src/modules/courses/courses.controller.ts`  
**Severity:** 🟡  
Два последовательных запроса к БД не обёрнуты в транзакцию. Если `assignInstructor` упадёт — курс создан но без инструктора. Инструктор-создатель его больше не увидит в своём списке.

---

### [2026-07-31] Password reset — нереализованная заглушка на публичном endpoint
**Файл:** `apps/api/src/modules/auth/auth.controller.ts`  
**Severity:** 🟡  
`confirmPasswordReset()` принимает любые данные и возвращает 200 без действий. Endpoint публичный. Пользователь не получит ошибку при попытке сбросить пароль.

---

### [2026-07-31] Rate limit 429 использует raw JSON вместо NestJS error формата
**Файл:** `apps/api/src/common/middleware/api-hardening.ts`  
**Severity:** 🟢  
Middleware находится до NestJS pipeline — формат ошибки 429 отличается от остальных API ошибок. Клиент получает разные структуры ответа при разных ошибках.

---

### [2026-07-31] Login возвращает `accessToken` и в теле и в cookie одновременно
**Файл:** `apps/api/src/modules/auth/auth.controller.ts`  
**Severity:** 🟢  
После PR 137 refresh token убран из тела — правильно. Но access token дублируется: и в Set-Cookie и в JSON body. При переходе на cookie-only — избыточность.

---

### [2026-08-05] Сайдбар админки разъезжается на 2 колонки при зуме браузера 150%+ — причина не найдена
**Файл:** `apps/web/src/styles/admin.css` (`.admin-layout`, `.admin-sidebar`, `.admin-nav`), `apps/web/src/shared/adminPage.tsx`
**Severity:** 🟡
На проде (`web-production-b1f01.up.railway.app/admin/*`) при зуме браузера (Ctrl +/-, не сужении окна) от ~150% и выше пункты меню «Управление» и «Настройки» рисуются двумя колонками рядом вместо одного вертикального списка — при этом верхняя шапка остаётся десктопной (мобильный breakpoint не срабатывает). Подтверждено скриншотами пользователя на 250% зуме, воспроизводится стабильно, переживает жёсткую перезагрузку (Ctrl+Shift+R) — не кеш.

PR #492 (`fix(admin): contain horizontal overflow in admin layout at high browser zoom`) устранил горизонтальный скролл всей страницы при зуме, но **не** устранил именно это разъезжание на 2 колонки — это оказался отдельный, ещё не объяснённый симптом.

**Что уже проверено и исключено:**
- Деплой актуальный — подтверждено через Railway API, что на проде именно коммит из #492.
- Не кеш — воспроизводится после hard refresh.
- В `admin.css` нет `column-count`/`columns`/`flex-wrap`/`flex-direction: row`, которые могли бы объяснить раскладку в 2 колонки у `.admin-nav` (grep по всему файлу).
- 3 способа эмулировать зум Chrome в headless-браузере (сужение viewport, CSS `zoom`, CDP `Emulation.setPageScaleFactor`) не воспроизвели баг на локальной копии той же вёрстки/стилей — то есть либо баг специфичен для настоящего Ctrl+/- зума (не эмулируется программно так, как я пробовал), либо в игру вступает что-то, чего нет в упрощённой копии.
- Нет прямого сетевого доступа к живому URL из агентской песочницы (403), поэтому нельзя было сверить реально отданный CSS-бандл побайтово с локальным.

**Что нужно сделать, чтобы закрыть:**
1. Получить от пользователя скриншот панели DevTools → вкладка **Computed** для элемента `.admin-nav` в момент бага (250% зум) — это даст реальные computed-стили вместо гипотез.
2. Если `display`/`flex-direction` в Computed окажутся ожидаемыми (`flex`/`column`) несмотря на визуальный баг — проверить `.admin-sidebar`/`.admin-nav` на предмет **known Chromium-бага** с округлением/рендерингом flex-контейнеров при дробном zoom factor (баги такого рода фиксируются в Chromium bug tracker, обычно лечатся через `transform: translateZ(0)`/`will-change` форсирование отдельного composite-слоя, либо через `contain: layout`).
3. Проверить сценарий на другом браузере (Firefox/Safari) — если баг только в Chrome, это сильно сужает круг причин.
4. Проверить, не влияет ли `AccountSwitcher`/`LanguageSwitcher` (свои CSS-файлы, не входящие в `admin.css`) на layout сайдбара при зуме — не проверялось.

---

### [2026-08-05] E2E-тест «expired access cookie is refreshed» флейкует на CI → возможная причина найдена, проверяется на практике
**Файл:** `apps/e2e/tests/login-role-redirect.spec.ts:63`
**Severity:** 🟡
Тест падал по таймауту на CI 4 раза подряд с разной локацией фейла каждый раз (`refreshSucceeded`, потом дважды `expiredAccessRejected`) — признак таймингового флейка, а не детерминированного бага в коде. Два предыдущих расследования (`be8802f`, `e5cad53`) проверяли только код (`apiClient.ts`, `auth.controller.ts`, `auth.cookies.ts`) и признали его корректным — но не заглядывали в конфиг тестового харнесса.

**Найдено (2026-08-05, PR `claude/e2e-api-prod-build`):** `apps/e2e/playwright.config.ts` поднимал API для E2E через `pnpm --filter @lms/api dev` (`nest start --watch`) — dev-режим с live-компиляцией через ts-node/webpack и файловым watcher'ом, — хотя строчкой раньше уже собирался нормальный `dist/main.js`, который просто не использовался. В проде (`apps/api/Dockerfile`) сервер стартует иначе: `node dist/main.js`, без watcher'а. На CI-раннере (ограниченный CPU) watch-компилятор конкурирует за процессор с 2 параллельными Playwright-воркерами — именно у этого теста самый узкий тайминг-бюджет во всём наборе (3 последовательных round-trip'а подряд), поэтому он чувствительнее остальных к такой конкуренции.

**Фикс 1:** добавлен скрипт `apps/api/package.json` → `start:prod`: `node dist/main.js`; `playwright.config.ts` теперь запускает именно его вместо `nest start --watch` — тот же бинарник, что и в проде.

**Промежуточный результат:** следующий прогон на CI — прогресс, но не закрыто. Тест снова упал, но **на более поздних шагах** (`refreshSucceeded`/`retrySucceeded`, а не `expiredAccessRejected` как раньше), и весь набор из 20 тестов стал заметно быстрее (57.7s против прежних 1.2m) — подтверждает, что `dist/main.js` реально снял часть накладных расходов. Но общего 30-секундного лимита теста (Playwright default `test.setTimeout`) на 3 последовательных round-trip'а всё ещё впритык — этот лимит никогда раньше явно не поднимали (только внутренний `expect.poll` до 10с в `be8802f`, отдельно от общего таймаута теста).

**Фикс 2:** добавлен `test.setTimeout(60_000)` персонально для этого теста в `login-role-redirect.spec.ts` — не общий таймаут всего набора, только для теста с объективно самым узким тайминг-бюджетом.

**Статус:** оба фикса обоснованные и по отдельности подтверждённо помогают (быстрее сервер + больше времени именно там, где оно нужно), но 100% гарантии, что флейк не повторится, дать нельзя — CI-раннер с его ограничением CPU локально не воспроизвести. Считать закрытым только после нескольких зелёных прогонов `test:e2e` подряд без повторного падения этого теста. Если продолжит падать — см. пункты 2–3 из предыдущей версии этой записи (изоляция `--workers=1`, проверка event loop API) как следующий шаг.

---

### [2026-08-05] Admin → Роли: кнопка «Создать роль» и цвет badge не реализованы — сознательно, решение открыто
**Файл:** `apps/web/src/app/AdminRolesPage.tsx`, `docs/lms-ui-prototypes-complete/admin/lms-admin-roles-verified.html`
**Severity:** 🟢
При построчной сверке с прототипом на этой странице поправили только текст (`admin.roles.title`/`subtitle` — PR `claude/admin-roles-prototype-copy`). Два расхождения оставлены как есть, не молча пропущены:

- **Кнопка «Создать роль».** В прототипе — заглушка (`alert('Прототип: создание роли')`). В реальном API нет ресурса «роль»: роли — фиксированный enum (`learner/instructor/manager/admin`), назначаемый через `/memberships`, не создаваемая сущность. Подтверждено — `grep` по `apps/api/src` не находит контроллера/эндпоинта для ролей. Добавить кнопку без бэкенда значило бы показать нерабочий элемент управления.
- **Цвет badge со счётчиком.** В прототипе — индиго-заливка (`--ps`/`--p`), в коде — `ds-badge--neutral` (серый). В дизайн-системе (`ui.css`) нет цветного/primary-варианта `ds-badge` — только `neutral/published/draft/overdue/done/new/warning`. Менять ради одной страницы не стали.

**Что нужно решить:**
1. Нужна ли вообще функциональность «создать роль» (кастомные роли), или 4 фиксированные роли — окончательный дизайн системы? Если нужна — это отдельная задача с бэкенд-изменениями (новый ресурс + RBAC), не UI-правка.
2. Стоит ли заводить `ds-badge--primary`/`ds-badge--accent` в дизайн-системе для случаев вроде этого счётчика, или оставить нейтральный серый как есть.

---

## Закрытые

### [2026-07-31] Покрытие функций ниже threshold 25% в CI → **закрыто**
PR 172 — экспортированы `computeStats` и `buildStudentRows`, добавлен `InstructorUtils.spec.ts`. Coverage поднялась до 25.6%.

---
