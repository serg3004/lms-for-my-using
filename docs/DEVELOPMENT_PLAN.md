# План разработки

## Оргструктура — PR 282

**Статус: реализовано; DB integration локально не проверен из-за отсутствия Docker.** Добавлены метрики и безопасные structured diagnostics
для tree queries, manager scope, learning-target resolution, department reports,
CSV import и reparent conflicts. Релевантные проверки перечисляются в Pull
Request; production monitoring stack и domain/authorization semantics не менялись.
